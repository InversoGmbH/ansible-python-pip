---
# defaults file for inverso.python-pip

python_pip__package     : 'python-pip'
python_pip__config_file : '/etc/pip.conf'
python_pip__state       : 'present'
python_pip__trusted_cert: ''

python_pip__modules     : []

python_pip__args        : '' # --trusted-host pypi.org --trusted-host pypi.python.org --trusted-host  files.pythonhosted.org

python_pip__executable:
